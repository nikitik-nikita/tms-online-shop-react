import { createUseStyles } from 'react-jss';

const logoStyles = {
  logo: {
    color: '#ffffff',
    textDecoration: 'none',
  },
};

export default createUseStyles(logoStyles, { name: 'Logo', index: 304 });
